import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const contactsSlice = createSlice({
    name: 'contacts',
    initialState,
    extraReducers: {},
    reducers: {},
})